#include "kdtree.h"

mnist_case_t *find_median(UNUSED mnist_case_t *case_array, UNUSED size_t array_size, UNUSED size_t axis)
{
    fprintf(stderr, "Fonction 'find_median' non implémentée!\nFin du programme...");
    exit(1);
}

kdtree_t kdtree_build(UNUSED mnist_dataset_t dataset)
{
    fprintf(stderr, "Fonction 'kdtree_build' non implémentée!\nFin du programme...");
    exit(1);
}

void kdtree_delete(UNUSED kdtree_t t)
{
    fprintf(stderr, "Fonction 'kdtree_delete' non implémentée!\nFin du programme...");
    exit(1);
}

mnist_case_t **kdtree_knn(UNUSED kdtree_t tree, UNUSED mnist_picture_t t_case, UNUSED size_t array_size, UNUSED size_t k)
{
    fprintf(stderr, "Fonction 'kdtree_knn' non implémentée!\nFin du programme...");
    exit(1);
}